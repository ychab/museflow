# ROADMAP

This quick and dirty document just list the roadmap tasks.

## spotify history

- I have the feeling that we don't have the real history right? Looks like I miss a lot of songs...

## museflow

- the optimal workflow could be slightly different
    => import history
    => rate history first (at least 300 tracks)
    => THEN build the profile AND the profiler should take the rates into account!!
        => which means timeline + preferences

- command to create playlist based on the score? filtered by artist?

- command rate stats => list top artist? list top track? two separate command?

- review commands to exclude typer echo from command logic callback

- personnality inspection through the music history? Who are you based on your streaming history?

- add a quizz on random music to discover the taste of the end-user?

## claude

=> reduce the size of the CLAUDE.md no? Keep the minimum here (always loaded = tokens++)

## Tests

- check if we don't have redondant tests?

## advisor

- implements other cheapest and pertinent AI like:
    => openai?

## direnv

- add support? What about tests with env var?

## Just

Replace Makefile by Just?
