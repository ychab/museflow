# ROADMAP

This quick and dirty document just list the roadmap tasks.

## museflow

- store the provider + provider_id into a list on the track: when we rate a track, we don't care about the provider. Anyway, if we decide for example to delete all the tracks for a given provider (and user), we may just need to remove it from the list and keep it if another provider use it, or delete it if it was the only one. If we did this, then the unicity could be just user_id + fingerprint.

- looks like we have some duplicate in the history track no? check fingerprint? e.g:
    => Ma meilleure amie => Vald (at least, I rate it twice)
    => Chef - Caballero & JeanJass
    => Booba - Drapeau noir
    => PLK - Hier
    => Booba - Petite fille
    => Koba LaD - Pas de reine
    => Vald - Désaccordé
    => Kekra - Vréalité
    => Zumbale Orquesta — Barranquillera de Puro Corazón
    => Kekra - Liste

- sometimes, the music doesn't exists anymore in the provider and we can't rate it with play... so we put 0 as a score

- add command track complete to detect: genres and mood with AI?

- command playlist delete should also (optionally) delete the remote playlist (provider)?

- personnality inspection through the music history? Who are you based on your streaming history?

- add a quizz on random music to discover the taste of the end-user?

## claude

=> reduce the size of the CLAUDE.md no? Keep the minimum here (always loaded = tokens++)

## Tests

- check if we don't have redondant tests?

## advisor

- implements other cheapest and pertinent AI like:
    => openai?

## direnv

- add support? What about tests with env var?

## Just

Replace Makefile by Just?
