# ROADMAP

This quick and dirty document just list the roadmap tasks.

## track entity

- genre: how to populate it? Copied from the artists?

## test

- fix flaky test: tests/integration/infrastructure/adapters/providers/spotify/test_library.py::TestSpotifyLibrary::test__get_saved_tracks__nominal - httpx.LocalProtocolError: Illegal header value b'bearer '
  ==> probably polyfactory the guilty (don't generate a token?)!
- fix flaky test: tests/integration/infrastructure/entrypoints/api/v1/endpoints/test_spotify.py::TestSpotifyCallback::test__spotify_error
  ==> my guess: polyfactory, for unknown reason yet, don't always generate a state value...?

## direnv

- add support?

## DDD

- rename domain/schemas to domain/inputs?

## ports

- remove make_api_call from ports?

## doc code

- improve the doc code for tests

## Entities/ models Music

- should we add a relationship between the track with the (top) artist?
- should we have a slug based on the track name + artist name?

## cli

- review how commands/logics are setup

## Sync

- review it for performance (small chunks to upsert into DB) and atomicity?
- spotify: import the extended streaming history?

## advisor

- to prevent duplicate with reconcile, better to use slug maybe? or get ALL reconciled?
- improve doc
- provide a max-retry arg? And so, review the current impl to retry if no result?

- openai?
- gemini?

- found other providers for recommendations (listenbrainz or musicbrainz)?
    => tastedive API

## Playlist (recommendations)

- spotify: create a user playlist

## direnv

- add support? What about tests with env var?

## Just

Replace Makefile by Just
