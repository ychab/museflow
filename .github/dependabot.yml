version: 2
updates:
  # Maintain Python dependencies
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "weekly"
    ignore:
      # pydantic-core is version-locked to pydantic internals; bump it via pydantic, not independently
      - dependency-name: "pydantic-core"
    # Grouping keeps your PR list clean by putting all "chore" updates in one PR
    groups:
      python-dependencies:
        patterns:
          - "*"
    labels:
      - "dependencies"
      - "chore"
    commit-message:
      prefix: "chore"

  # Maintain GitHub Action versions
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    labels:
      - "chore"
